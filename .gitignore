# 1. Бинарники (скомпилированные файлы)
# Игнорируем все файлы без расширения, которые могут быть бинарниками Go,
# но чтобы не игнорировать случайно системные файлы, лучше явнее:
*.exe
*.exe~
*.dll
*.so
*.dylib

# Игнорируем сам результат компиляции (имя вашего приложения)
# Если ваш бинарник называется 'myapp', раскомментируйте:
# myapp
discord-canary.tar.gz

# 2. Тестовые файлы
*.test
*.out
coverage.txt

# 3. Go workspace file (файлы рабочей области)
go.work
go.work.sum

# 4. Зависимости (обычно vendor не коммитят, если используется go mod)
vendor/

# 5. IDE и редакторы (VS Code, JetBrains, Sublime)
.idea/
.vscode/
*.swp
*.swo
*~

# 6. Системные файлы (macOS, Linux)
.DS_Store
Thumbs.db

# 7. Логи и временные файлы
*.log
tmp/